# fov-3d-vision

A foveated stereo rendering engine for Blender, and the active loop that uses it.

Two eyes on a fixed head, each a Cycles camera that samples the scene on a log-polar warp —
dense at the fovea, coarse in the periphery, 51× fewer rays than a uniform image of the same
field — verged on a point, rendering a pair in 30 ms at the `small` profile and 270 ms at
`full` on an RTX 4090. On top of it, in one command: a stereo field that turns a pair into
inverse depth with an uncertainty everywhere the pair looked, a belief on the head sphere that
fuses the pairs, and a policy that chooses the next fixation from what is known.

    blender -b scenes/classroom/classroom_eye.blend -P tools/active_loop.py -- \
        --out previews/loop/classroom --profile full --policy coverage --fixations 50
    .venv/bin/python tools/active_eval.py previews/loop/classroom

Fifty fixations of the Classroom at `full`: 1.3 G rays, 68–79 s, 92% of a 60° field of regard
measured, 8–9% of it at foveal quality, median inverse-depth error 0.04 /m overall and
0.015–0.019 /m where the fovea has been (10–14 cm at 2.7 m), the vergence for each fixation
taken from the periphery of the ones before. The four-panel figure — posterior depth with the
scanpath, its uncertainty, its error, the curves against rays — is `loop_fig.png` in the run.

The premise: a camera samples uniformly and an eye does not. Foveation is a bandwidth decision
before it is an attention mechanism, so the question is what a controlled, gaze-directed
sampling of a scene costs and what it buys. The answer this repository reached, phase by
phase: at the targets, foveation beats uniform sampling at equal rays at every budget (Phase
A); the disparity a pair yields is flat in the warp's shape at the scale the samples support,
so the cheaper warp wins per ray (Phase B); and in the loop, at equal rays, spreading buys
error and the objective does not — random, coverage-first and expected information end within
12% of each other and target order well behind, on both scenes and both profiles (Phase C).
Coverage-first is the default policy for that reason.

This is the fourth of a sequence: `bioeye` (the loop that ran, on a uniform sensor),
`active-stereo` (the framework, whose loop never ran), `bio-3d-vision` (thirteen experiments
that found coverage was what a loop buys and named the foveated sensor as the untested
form), and this. The summaries are `docs/phase-a-summary.md`, `docs/phase-b-summary.md`,
`docs/phase-c-summary.md`; the reports on the sequence are VISGRAF TR-09-2026 and the
methodology notes it cites.

## What is here

| | tool | what it does |
|---|---|---|
| sensor | `render_foveated.py`, `foveated_camera.osl`, `warp.py` | the log-polar warp as a Cycles camera; `warp.py` is the pure-numpy definition both interpreters import |
| rig | `rig.py`, `fixation_pairs.py` | two eyes at ±ipd/2 on the head's X; a verged pair at a world point; the epipolar frame on the sphere; `PairRenderer` for a session |
| record | `fixation_sequence.py`, `check_pairs.py`, `stereo_truth.py` | the D1 sample record (direction, value, footprint, distance per ray), its checks, and the ground-truth correspondence from the Position pass |
| field | `stereo_field.py` (`stereo_instrument.py` beneath it) | a pair → inverse depth per cell with a two-part variance, at five scales from 0.2° to 3.2°, with left–right consistency |
| loop | `belief.py`, `active_loop.py`, `active_eval.py` | the belief on the head sphere, the policies (targets, random, coverage, info, oracle), the loop in one Blender session, the replay and the figures |
| integration | `integrate_sphere.py`, `preview360.py`, `noise_floor.py` | finest-owns integration of a sequence, the references and the noise floor Phase A judged against |
| views | `sphere_views.py` | (RGB, depth) of a scene as it is and as the engine saw it, equirectangular and on the epipolar sphere, from a loop run and a `preview360` panorama at the eye; the engine's depth drawn by confidence |

Every tool has a check that can fail and most have a control; `--self-test` on the numpy
ones. `DECISIONS.md` holds the nineteen decisions and what would overturn each;
`docs/log.md` the dated record; `docs/<step>.md` one note per step with its Results as the
run left them.

Throughout, the head is fixed. Eyes rotate about their own centres.

## Layout

    CLAUDE.md       how we work
    DECISIONS.md    what is settled, and what would unsettle it
    docs/log.md     dated record of what was run and what came out
    docs/           one note per step; docs/phase-{a,b,c}-summary.md are the phase summaries,
                    docs/phase-a-result.md the result page; docs/reference/ holds baseline images;
                    docs/reviews/ third-party reviews with their reading
    tools/          scripts, all runnable from the repository root; warp.py and rig.py are pure numpy
                (both interpreters import them; each has --self-test)
    scenes/         scene sources; only manifest.json and asset.json are committed
    previews/       generated, gitignored

Nothing a command can rebuild is committed. Scenes and previews are absent by design:
`workshop_8k.exr` alone is 324 MB, and every scene here is either generated by a script or
downloaded by one, pinned by md5 or URL.

## Setup

Blender 5.2.1 LTS on `PATH` as `blender`, plus a small venv for `inspect_preview.py`:

    python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

## History — the three phases

### Phase A — monocular

One camera, one centre of projection.

| | Step | State |
|---|---|---|
| A1 | Scene sources: gather and check 3D scenes | **done** — three tiers verified on GPU; `docs/a1-scene-gathering.md` |
| A2 | Reference render at foveal spacing, plus the uniform-cost baseline and the noise floor | **done** — both references at 8192 spp, 32.3 / 35.8 min measured; D7 met on the Classroom, 2-5% short on the calib room; `docs/a2-reference-and-noise-floor.md` |
| A3 | Single foveated image: the warp, first in numpy, then as a Cycles camera | **done** — OSL camera verified on GPU; `docs/a3-foveated-camera.md` |
| A4 | A sequence of fixations | **done** — `fixation_sequence.py` writes the D1 record at 15 ms (small) and 140 ms (full) per fixation; warp, footprint, reader and control checks pass at both profiles, the binned foveal check is registration-limited on the cards; `docs/a4-fixation-sequence.md` |
| A5 | Integration of the sequence into a spherical representation | **done** — finest-owns integration and the D9 curve at both profiles with equal-budget uniform baselines; foveation wins at the targets, uniform over the sphere below the largest budget; `docs/phase-a-result.md` |
| A6 | The E2 and e_max sweep | **done, choice made in D16 (B3)** — at equal rays E2 4 wins the fixated targets and E2 1 the covered sphere; e_max 45 and 30; profiles unchanged (D11); `docs/a6-warp-sweep.md` |

The three warp parameters are settled as far as Phase A can settle them: s₀ is the profile
(0.1° small, 0.05° full), and E₂ = 2°, e_max = 45° stand under D11 after the A6 sweep, which
found that a larger E₂ buys foveal accuracy per ray and a smaller one buys coverage, so the
choice belongs to the objective.

### Phase B — binocular

A second eye at a second fixed centre, plus vergence. With both centres fixed, the baseline
is fixed in the head frame, so the epipolar geometry is constant and rectification is one
change of coordinates on the sphere. Phase A's per-eye spherical maps are already the data
structure this needs.

| | Step | State |
|---|---|---|
| B1 | The second eye on the EYE rig, verged fixation pairs, the foveae-on-target check and its control | **done** 2026-09-15 — `fixation_pairs.py`, `check_pairs.py`, `rig.py`, `warp.py`; D12; results in `docs/b1-verged-pairs.md`: all checks pass on both profiles, (e) within 0.002 spacings, control matches prediction to 3 µm; 29 ms / 271 ms per pair |
| B2 | Ground-truth stereo correspondence from the Position pass, epipolar coordinates on the sphere, the triangulation check and its control; per-eye references | **done** 2026-09-15 — `stereo_truth.py`, `preview360.py --eye-offset`; D13, D14; results in `docs/b2-stereo-truth.md`: (i), (k) at 0.015 s₀, (j) 100%, (h) within 0.006 quanta on both profiles, naive control inf on every card; small per-eye references pinned (full ones not rendered); per-eye (b) 36/35 of 50 against Phase A's 40, wires 6/8 and 5/8 read as lattice phase |
| B3 | The E₂ / e_max sweep with the disparity error as the objective (D11): a reference matcher as an instrument, and the matcher-free information bound beside it | **done** 2026-09-15 — `stereo_instrument.py`, `stereo_sweep.py`; D15; D16 closes D11: E₂ = 2, e_max = 45 as the middle of a flat optimum (instrument 0.272 / 0.269 / 0.261 s₀, bound 0.121 / 0.085 / 0.088 s₀ for E₂ 1 / 2 / 4); `docs/b3-stereo-instrument.md` |

### Phase C — the active loop

Observe a pair, infer depth with an uncertainty everywhere the pair looked, choose the next
fixation from what is known, observe again (D17). The loop lives here, on the Phase B rig and
record; the end of the phase is a foveated stereo rendering engine for Blender that runs it in
one command. bioeye is the model — a running loop and a four-panel figure — and
bio-3d-vision's foreclosures are the prior: coverage is what a loop buys, not looking twice
matters more than the objective.

| | Step | State |
|---|---|---|
| C1 | The stereo field: the D15 instrument extended over the whole disc a pair covers, level by level at the scale the samples support, with left–right consistency and an inverse-depth measurement with variance per cell | **done** — `stereo_field.py`; D17; `docs/c1-stereo-field.md`: (p) −7% at small, −24% at full; floor 0.10–0.37 cells |
| C2 | The loop: `PairRenderer`, a belief on the head sphere (`belief.py`: two variances, gating, the visit map), five policies (targets, random, coverage, info, oracle), one Blender session (`active_loop.py`), bioeye's four panels and the comparison (`active_eval.py`); D18 the evaluation contract | **done** — four runs, `docs/c2-active-loop.md`; (s)–(v) pass on all five at 50 fixations; error: info 0.103 < coverage 0.105 < random 0.111 < targets 0.140 < oracle 0.146 /m; fine coverage: targets 0.201 > coverage 0.138 > random 0.116 > info 0.080 > oracle 0.074 |
| C3 | Closing: the classroom at both profiles, `docs/phase-c-summary.md`, this front page; D19 | **done** — thirteen runs pass every check; on the classroom at `full` random 0.0385 < coverage 0.0427 < info 0.0435 < oracle 0.0453 /m, coverage-first first on fine coverage everywhere but the calib room; `docs/c3-closing.md` |

### Phase D — a reliable multiscale depth from the field

Whether the existing field can be made reliable: find where the gross errors come from,
remove the removable ones, represent the rest (D20, `docs/phase-d-plan.md`).

| | Step | State |
|---|---|---|
| D1a | The diagnosis on the saved records: occluded / window / search / resolution per level, P(gross) against the distance to a depth edge, the parent oracle (what coarse-to-fine could cure) | **run** 2026-09-18 — `gross_diagnosis.py`, `docs/d1-gross-diagnosis.md`; all checks pass on the four records; by area resolution is 75–89% of the bad cells, the oracle cures 3–25% of the wrong peaks at levels 0–2; D20's rule and the order D1b/D2 to be decided |
| D1b | Coarse-to-fine | **not written** (D21): the parent oracle is its ceiling, 16% of the wrong peaks on the classroom at `full`, net harmful at levels 1–2 on the calib room |
| D2a | The judge split (gross = coarse + outlier) re-judged on the saved runs; what tells a wrong peak from a right one (five features, per level); the 3 × 3 window as a what-if | **run** 2026-09-18 — thirteen runs re-judged, (s)–(v) hold, outlier 0.10 of gross 0.31–0.37 on the classroom at `full` (the fine band's gross is all outlier); no feature rejects more than 44.5% of the wrong peaks at 90% kept on the classroom's levels 0–2 (parent the best there, AUC ≤ 0.71); the 3 × 3 window loses on both scenes at every level; D2b to be decided |
| D2b | The neighbour test: a cell's distance from the median parallax of its consistent neighbours, as a sixth feature and as a rejection (`--nb-tol`), offline what-ifs, then the loop once if the offline rule is met | **run** 2026-09-18 — `neighbours` is the best single feature on the classroom at every level (49 / 50 / 44% of the wrong peaks at 90% kept, levels 0–2); as a rejection at one cell it cuts the wrong-peak fraction by 32 / 32 / 23% at levels 0–2 for 2% of the right peaks; **the offline rule is not met** (a third at each level) by `nb1` or `nb1i`, the loop was not run; D2 closes: at one look the outliers cannot be told from the matcher's own evidence; D4 next |
| D4 | The run to saturation (coverage-first, `full`, 500 fixations, read at checkpoints) and, on its record, a second look as the test: repeatability of bad looks, agreement, mean / median / consensus fusion (`second_look.py`); coverage-first continues as least-looked once the cap is covered | **run** 2026-09-18 — 500 fixations in 12.0 min, least-looked from k = 146, 469 distinct directions; fine coverage 0.094 → 0.295, median ρ error flat at 0.039 from K = 200 (twice the fine band's 0.019), gross 0.336 = coarse 0.200 + outlier 0.136; bad looks repeat (P(second bad | first bad) 62% against 11%), consensus fusion 18.3% fine gross against the mean's 27.9% with 9.3% undecided; (z1) fails by 1422 cells the belief's gate dropped (diagnosed, left); record backed up outside the checkout |
| D5 | The consensus belief: a majority among the fine looks of a cell, undecided cells fall back to the coarse stream and are exported; `--fusion consensus` in the loop, `active_eval.py --refuse consensus` re-fuses a record without rendering; D23 | **run** 2026-09-18 — re-fused on D4's record at K = 500: fine outlier 0.206 → 0.171, outlier 0.136 → 0.126, fine coverage 0.295 → 0.263 (undecided 3.2% of the cap), ρ error 0.0387 → 0.0365 (fine 0.0187 → 0.0151), z RMS 1.66 → 1.27; **the rule lands in between** (outlier misses ≤ 0.125 by 0.0007, fine coverage ≥ 0.265 by 0.0025); no live run, `--fusion` stays `mean` by default; Phase D closes |

## State

Phases A, B and C are complete and summarised; Phase C ended 2026-09-17 with the engine
above and the finding that, at equal rays, the policy that does not look twice is as good as
the principled one (D19). Phase D is complete and summarised (`docs/phase-d-summary.md`, D21–D23): three quarters of
what the loop called gross was coarse right peaks inside the variance model; the real outliers
are a tenth of the area, of which about 8% — depth edges and half-occlusions — is the block
matcher's floor at one look; and two looks are a test where one is not, so a consensus among a
cell's fine looks (`--fusion consensus`, an option) takes the fine band's error from 0.0187 to
0.0151 /m on the same 500 fixations. Still open beyond it: the warp as an action, the fixed head, truth beyond what the eyes
sampled.

The scene tooling was first developed in the `visgraf/w3d-scenes` repository and has been
folded in here; see D6 in `DECISIONS.md`.


<!-- FSG1_HANDOFF_20260919 -->
## Foveal Surface Growing - FSG1..FSG7a, Stage II Scene-1a/1b/1c, Reality Checks 1-2b, Cyclopean-1a..1g, then MultiObject-1a..3a (opt-in experiment)

| Step | State |
| --- | --- |
| FSG1: single-patch local stereo | **COMPLETE - Increment 1 closed 2026-09-19 (`FSG1H_FINAL_VALIDATION_PASS`).** The FSG1 local RGB-D instrument is `FSG1-HDR-SGBM-one-original-update-original-validity-v1`: fixed soft-HDR encoding -> unchanged SGBM -> exactly one original photometric refinement update -> original validity predicate (no endpoint or footprint support vetoes). Validated prospectively on fresh fixtures and fresh seeds 101/149 (Blender 5.2.1, OPTIX, 2,516,582,400 primary camera samples) against gates frozen before the data existed: four disparity-phase planes at 22.00/22.25/22.50/22.75 px cover 99.77-99.97% at 0.085-0.226% median and 0.284-0.959% p95; occluder foregrounds 100.000% coverage; occluded backgrounds 93.2-95.2% (the tightest margin) at <=0.405% median and <=1.638% p95; boundary accuracy gates pass with ~4,000 accepted points each at <=0.330% median and <=2.052% p95; and **0 accepted points in all 11,592 singly-visible occlusion-core pixels**, 0 wrong-instance acceptances. Route there: FSG1b traced the original coverage miss to over-exposure, FSG1c fixed the encoding, FSG1d failed prospectively and first exercised half-occlusion, FSG1e showed the error tail was the +/-0.75 px refinement cap on near-integer phase, FSG1f narrowed the candidate, FSG1g stopped on a fixture defect (occluder edges outside the +/-6 deg core) which FSG1h corrected. Scope: one local patch under controlled opaque, diffuse, planar, calibrated conditions with oracle segmentation - no claim about arbitrary scenes, complete boundaries (~40% of boundary reference stays unaccepted), thin structure or calibrated uncertainty. Increment 2 is now complete; see the FSG2 row below. See `docs/fsg1h-final-validation.md` and the FSG1 document chain. |
| FSG2: two-patch surface fusion (Increment 2) | **COMPLETE 2026-09-19 (`FSG2_INCREMENT2_PASS`).** Two prescribed overlapping foveal patches (fixations yaw -3 and +3 deg) from the frozen FSG1 instrument accumulate into one persistent surface in the fixed head frame, with exact calibrated poses and **no registration, ICP, meshing or hole filling**. One full seed-211 acquisition (419,430,400 primary camera samples). Every prospective gate met: patch coverage 99.1% / 100.0%; 32,222 B points matched and 50.8% still new; matched A/B distance 1.907 mm median / 6.187 mm p95; fused point-to-true-plane 3.548 mm median / 9.740 mm p95; fixed-grid coverage **59.7% -> 90.2%**, a 30.5 pp gain; idempotent replay. The map holds 91,941 surfels, 16,860 with two-look support, containing only object ID 61. Scope: one finite planar tilted object under oracle segmentation, two fixations, one seed, and a prospectively fixed 12 mm Euclidean association rule - an engineering baseline, not a final surface model; folds, self-occlusion and calibrated uncertainty are deferred. Increment 3 is now complete; see the FSG3 row below. See `docs/fsg2-increment2.md`. |
| FSG3: active frontier growth (Increment 3) | **COMPLETE 2026-09-19 (`FSG3_INCREMENT3_PASS`).** A truth-free frontier policy, given one seed fixation, chose its own subsequent fixations from the evolving persistent map plus the current oracle segmentation frontier, and **stopped by itself at the visible object boundary**. One full seed-307 active run (1,048,576,000 primary camera samples): the policy produced 5 fixations at yaw -7, -2, +3, +8, +13 deg, every saccade exactly +5 deg, no repeat, terminating on `no_frontier` - the map's right extent reached +14.632 deg against the fixture's analytic +15.190 deg edge. Surface coverage grew **33.6% -> 100.0%** (+20.99/+20.18/+19.97/+5.24 pp, no step losing coverage) at **4.219 mm median** point-to-plane error (p95 13.587 mm); 92,632 surfels with support histogram {1: 39,551, 2: 47,660, 3: 5,421}; per-patch oracle coverage 94.1-96.7%; overlaps 25,720-26,608 matched at 1.8-2.0 mm median, all idempotent; every map point ID 71. The loop and policy were verified to import no fixture geometry and open no `evaluation_only` asset. Scope: **feasibility, not optimality - no competing gaze policy was evaluated**; one opaque diffuse planar tilted rectangle, oracle segmentation, horizontal saccades only, fixed 2.10 m vergence, one seed. Folds, self-occlusion, multi-object switching, head motion, vergence control and calibrated uncertainty remain open. See `docs/fsg3-increment3.md`. |
| FSG4: active vs fixed scan (Increment 4) | **COMPLETE - Increment 4 closed 2026-09-19 (`FSG4C_INCREMENT4_PASS`).** On two fresh opaque planar placements (`case_c`/`case_d`) x two fresh seeds (503/557), with exactly paired observations and equal-or-lower logical camera budget, the frozen frontier policy acquired visible surface more efficiently than the one frozen nonadaptive scan `0,-5,+5,-10,+10`: **AUC wins 4/4**, mean AUC advantage **0.1702**, mean final-coverage advantage **0.2218**, active reaching **100.0%** final coverage in all four against the scan's 73.3-82.3%. The policy found opposite directions on the mirrored placements unaided (case_c 0,-5,-10,-15; case_d 0,+5,+10,+15,+20; both `no_frontier`). Map quality holds for both policies (plane medians 4.31-4.52 mm, p95 13.59-14.81 mm, every map pure ID 81, every fusion idempotent); active received no cache, proven by newly-rendered equalling logical samples. Route there: FSG4a stopped at the paired-noise gate (OptiX renders are not bit-reproducible); FSG4b restored exact pairing by reusing the active acquisition artifact **without relaxing the gate** and won 4/4, but formally FAILED on an inherited per-fixation terminal rule that is unsatisfiable once 98.7% is already covered - that FAIL is preserved; FSG4c made per-fixation novelty/gain descriptive (residual closure was 100.0% at every terminal fixation) and revalidated on fresh fixtures and seeds. **The correction was not load-bearing: the terminal numbers would have passed the retired rules anyway.** Scope: not policy optimality, not a population estimate, one comparison scan, oracle segmentation, horizontal saccades, fixed 2.10 m vergence. **The next experiment is authorized but NOT implemented.** See `docs/fsg4-increment4.md`, `docs/fsg4b-pairing-reuse.md`, `docs/fsg4c-increment4.md`. |
| FSG5: curved-surface growth (Increment 5) | **COMPLETE - Increment 5 closed 2026-09-20 (`FSG5_INCREMENT5_PASS`, 4/4 trials).** A curvature stress test: geometry changed, intelligence did not. With the **frozen FSG1 instrument, the unchanged `fsg4_policy` and the unchanged `fsg3_surface_map` 12 mm Euclidean fusion** - no new policy, no registration, no ICP, no meshing, no hole filling - the active loop grew a convex cylindrical ribbon (radius 0.75 m, 75 deg arc) to **~100% curved-surface completeness** at **3.36-3.65 mm median** and 10.77-11.31 mm p95 point-to-surface error, on both mirror orientations and both fresh seeds (601/647), 4,194,304,000 primary camera samples. The policy produced the mirrored trajectory on the mirrored fixture **unaided** (curve_right -7,-2,+3,+8,+13; curve_left +7,+2,-3,-8,-13), all terminating `no_frontier`. The curvature-specific gate passed in the safe direction: **signed radial median +1.25 to +1.34 mm** on 54.5-55.4k multi-look surfels against a +/-7.5 mm limit - positive, so the feared inward contraction under Euclidean fusion did **not** occur; an independent check of the exported clouds gives median radius 0.75075-0.75097 m against a true 0.750 m. Every map pure ID 81, every replay idempotent. Scope: two mirrored ribbons of ONE radius and arc, two seeds, oracle segmentation, exact poses, horizontal saccades, fixed vergence - not general curvature, varying radius, concave or saddle geometry, self-occlusion, folds or multi-object scenes. The policy is still a 2D image-edge/map-yaw controller; a true 3D surface-frontier controller is the clean next question and was deliberately not built. **That next experiment was implemented and run as FSG6, which FAILED 2/4; see the FSG6 row below.** See `docs/fsg5-increment5.md`. |
| FSG6: 3D surface frontier (Increment 6) | **FAILED 2026-09-20 (`FSG6_INCREMENT6_FAIL`, 2/4 trials). Preserved and returned to Luiz/Chat; Increment 6 is NOT closed and no next experiment is authorized.** The horizontal image-edge controller was replaced by a truth-free frontier extracted from the persistent 3D surfel map itself (25 mm voxels, 65 mm neighbourhood covariance, smallest PCA eigenvector as normal, missing direction = negative tangent-projected mean neighbour offset; eight-neighbour 5-degree yaw/pitch lattice; oracle segmentation demoted to a current-view object-continuation veto). The FSG1 instrument and `fsg3_surface_map.py` were unchanged (`git diff 3d5125c` empty). **The representation worked**: on all four full trials (5,033,164,800 primary camera samples) it drove a genuinely two-dimensional trajectory - **pitch span 15.0 deg on every trial** against a >=10 deg gate, where a five-look horizontal-only scan reaches only 0.412 ideal coverage on this fixture - and grew a rolled cylindrical ribbon to **~99.1% curved-surface completeness at 4.04-4.64 mm median** (p95 12.78-14.45 mm), nonterminal frontier support 21-93 surfels against >=8, every map pure instance 91, every replay idempotent, signed radial +1.42 to +1.69 mm (outward, no inward contraction, reproducing FSG5). `diag_down_left` passed on both seeds terminating `no_frontier`; **`diag_up_right` failed on both seeds** with exactly two fails each - termination `max_fixations` instead of `no_frontier`, and `fix_04` object measurement coverage 0.8940 against >=0.90. Root cause, measured: **the mirror pair is not a monocular mirror.** The fixtures are an exact 180-degree image-plane rotation as geometry (`up_right L` vs rotated `down_left R`: **0 of 409,600 pixels disagreeing, IoU 1.000000**), but that roll maps (x,y,z) to (-x,-y,z) and so **swaps the two eye centres**; the controller reads only the left-eye oracle mask, so the mirrored fixtures differ by the full binocular parallax - **37.4 px (1.76 deg) on a ribbon 119 px (5.59 deg) wide**. The 0.15 edge-band veto turns that into a trajectory difference (mirrored edge fraction systematically 0.136-0.143 lower; at the third fixation **0.0703 vs 0.2066**), vetoing the (+5,+5) diagonal, deflecting the controller onto a pure-yaw move, and both FAIL lines follow mechanically. Reproduced identically on both seeds with byte-identical oracle reference counts. Repair needs a change to the fixture design, the reference eye or `edge_object_fraction_min` - **a specification question not delegated to Code**, so nothing was tuned. One code fix, the only one: `fsg6_run.py` line 64 referenced an undefined name `z` in the loop's revisit guard, crashing every step after the seed; changed to `gaze` (accepted and retained). The eye-symmetry repair was then implemented and validated as FSG6b, which also FAILED 2/4 on a different cause; see the FSG6b row below. See `docs/fsg6-increment6.md`. |
| FSG6b: binocular continuation veto (Increment 6) | **FAILED 2026-09-20 (`FSG6B_INCREMENT6_FAIL`, 2/4 trials). Preserved and returned to Luiz/Chat; Increment 6 is NOT closed and no next experiment is authorized.** FSG6b changed exactly one architectural element: the segmentation-only physical-boundary veto became binocular and eye-swap invariant, using `f_cont(d) = max(f_L(d), f_R(d))` against the **unchanged 0.15** threshold. `check_fsg6b.py` reports `passed=8 failed=0` with `eye_swap_invariant=true`, its `monocular` negative proves the retired left-eye-only rule asymmetric, and the FSG6a frontier constants and gates are verified **exactly equal** (`SURFACE_FRONTIER`/`TARGETS` identical, no differing keys). The right eye came from outputs the frozen FSG1 instrument already produced (`compute_once`'s third `state`, `support_mask(...,'R')`) - **no instrument change**. **The repair works and is not what failed**: at fixation 0 of both `fresh_up_right` trials f_L=0.0000 / f_R=0.2414 permitted a direction the old rule vetoed, FSG6a's eye-asymmetry mode did not recur, and FSG6a's `fix_04` coverage miss is gone (patches 0.9215-0.9485 vs >=0.90); honestly, that rescue fired twice and changed no selected move. On two fresh, deliberately **non-mirror** ribbons x seeds 809/853 (5,033,164,800 samples) all four trials held pitch span **15.0 deg** (gate >=10), frontier support 29-90 (>=8), pure instance 101, idempotent replay, radial bias -0.30 to +0.99 mm, and reached **99.2-99.7% coverage at 3.35-4.27 mm median**. `fresh_down_left` passed on both seeds (`no_frontier`); **`fresh_up_right` failed on both with a single fail line each** - termination `max_fixations`. Root cause, measured: **the conjunctive corner rule**. A diagonal move requires BOTH edge bands to continue, but a ribbon rolled +30 deg exits the fovea through a **corner**: at gaze (+2,+3) the top band is L=0.0000/R=0.0035 (correctly vetoed - the object occupies rows 25..255 and never reaches the top row, both eyes agreeing) while **the top half of the right band is 0.7883/0.9258 object**. The (+5,+5) move is blocked, the controller detours pure-yaw, and still has two eligible candidates when the 6-fixation budget ends. A **specification question, not an implementation defect** - the conjunctive rule is written into the handoff and frozen - so nothing was tuned and **no code fix was required or made**. That rule was then replaced and validated as FSG6c, which also FAILED, on the opposite defect; see the FSG6c row below. See `docs/fsg6b-increment6.md`. |
| FSG6c: candidate-aligned continuation (Increment 6) | **FAILED 2026-09-20 (`FSG6C_INCREMENT6_FAIL`). The diagnostic smoke raised a RUNTIME EXCEPTION, which blocks the full schedule, so NO full trial was run. Preserved and returned to Luiz/Chat; Increment 6 is NOT closed and no next experiment is authorized.** FSG6c replaced the retired diagonal component-conjunction with candidate-aligned forward-perimeter continuation: `f_cont(d) = max(f(edge) for edge in compatible_edges(d))` against the **unchanged 0.15** threshold, axis-aligned behaviour identical to FSG6b. **Everything upstream of acquisition passed**: `[fsg6c-check] SUMMARY passed=10 failed=0`, all nine negatives exit 1 (including `conjunction` and `monocular`), all sixteen FSG1-FSG6b suites green, `SURFACE_FRONTIER`/`TARGETS` **exactly equal** to FSG6b. The new evaluator-side preflight - continuous analytic cylinder -> the repository's own rectification/crop/support masks -> the **exact runtime continuation function** - verified the load-bearing control on real masks: at `corner_up_right` gaze (2,+3) the (+5,+5) transition measures right=0.6219, top=0.0797, permitted by FSG6c and rejected by the retired conjunction, so **the FSG6b design/runtime inconsistency is genuinely fixed**. **But the policy walked off the fixture**: (-8,-7) -> (-3,-12) -> (-8,-17) with 7,005 then 1,486 then **0** object pixels, against a ribbon spanning pitch [-10.19,+9.95]. Root cause, measured: **`max()` lets one component license the other**. At the seed gaze the bottom edge is **0.0000 in both eyes** - a provably resolved boundary - yet down-right `(+1,-1)` has compatible edges {right,bottom} and `max(0.4203, 0.0000) = 0.4203 >= 0.15` permits it, while the retired rule rejects it. Reproduced through the analytic preflight at **both profiles and both fixtures**; the oracle mask is ray-traced and seed-independent, so all four full trials would have started from this state - geometric and deterministic, not a Monte-Carlo accident. **FSG6c is too permissive in precisely the dual way FSG6a/FSG6b were too strict**: the conjunction demanded continuation on every component, `max()` demands it on none in particular, and neither expresses "the surface leaves through the corner region between these two edges". **No code fix was made or permitted** - the implementation matches the written rule exactly, so this is a specification consequence, not an implementation defect. Also recorded: the shipped over-permissiveness guard resolves BOTH compatible edges; a control with one edge exactly zero and the other strong would have failed before acquisition. That combination rule was then replaced and validated as FSG6d, which fixed the veto but FAILED on termination; see the FSG6d row below. See `docs/fsg6c-increment6.md`. |
| FSG6d: projected frontier exit corridors (Increment 6) | **FAILED 2026-09-20 (`FSG6D_INCREMENT6_FAIL`, 0/4 trials). Preserved and returned to Luiz/Chat; Increment 6 is NOT closed and no next experiment is authorized.** Every trial fails on **exactly one gate, the same one**: termination `max_fixations` instead of `no_frontier`. FSG6d changed the continuation veto's **spatial support, not its threshold**: each candidate's own supporting 3D frontier surfels and their existing look-ahead targets are projected into both rectified cores, the rays extended to the boundary, only exits in the candidate's forward image sector kept, and object support measured in the terminal corridor - corridor length and width both derived from the already-frozen `edge_band_fraction=0.04`, so **no new numerical constant**. `[fsg6d-check] passed=11 failed=0`, all eleven negatives exit 1 (including `conjunction`, `componentmax` and `full_edge`), all seventeen prior suites green, `SURFACE_FRONTIER`/`TARGETS` **exactly equal** to FSG6c. **The veto works and is not what failed**: at the third fixation of every trial it permitted the diagonal the retired FSG6b conjunction would have vetoed (corridor 0.7031 with full-edge **top=0.0000**; corridor 0.7787 with **bottom=0.0535**), coverage jumping 74.5→97.0% and 75.5→98.4% at that step, while the FSG6c walk-off did **not** recur - no trial ever left the ribbon and the veto still rejected 4 of 8 neighbours at every final fixation. It is the first FSG6 continuation rule that is simultaneously eye-symmetric, permissive to a genuine corner exit and restrictive against one-component licensing. **Every other gate passed on all four trials**: pitch span 15.0°, per-patch coverage 0.9114-0.9503, matched 11,387-20,222, overlap medians 1.97-3.20 mm, all idempotent, gain 60.4-63.5 pp, pure instance 121, final coverage **98.7-99.0% at 3.46-3.94 mm median**, and exported-cloud median radius **0.74001-0.74005 m vs true 0.740** and **0.70095-0.70096 m vs 0.700** - sub-millimetre, the best radial agreement of any FSG6 increment. Root cause, measured: **the 3D frontier never resolves on a thin ribbon**. Frontier counts run [207,214,206,189,157,164] while coverage goes 38.6→99.0% - the population never decays, because tangent asymmetry always sees the ribbon's long lateral boundaries across a 12° fovea. Termination can then only come from the candidate set emptying, and at the last fixation the survivors are **interior** lattice cells inside the already-swept region. Four fixations already suffice numerically (97.0-98.6% coverage), but the policy has no signal to stop. **A specification result, not an implementation defect - no code fix was made**: the failure is the FROZEN frontier-extraction termination behaviour meeting the FROZEN `no_frontier` gate and FROZEN six-fixation budget. **The open question is no longer the veto but termination.** That question was then addressed and validated as FSG6e, which demonstrated truth-free termination but FAILED 2/4 on robustness; see the FSG6e row below. See `docs/fsg6d-increment6.md`. |
| FSG6e: persistent OPEN frontier state (Increment 6) | **FAILED 2026-09-20 (`FSG6E_INCREMENT6_FAIL`, 2/4 trials). Preserved and returned to Luiz/Chat; Increment 6 is NOT closed and no next experiment is authorized.** FSG6e changed the **state of a raw frontier**, not the settled FSG6d corridor and not any gate: each frontier's existing 0.12 m look-ahead target is classified **MAP_RESOLVED** (within the frozen 12 mm FSG3 association of a mapped surfel), **BOUNDARY_RESOLVED** (a completed binocular fixation saw the projected target supported in **both** eyes with `max(f_L,f_R) < 0.15`), or **OPEN**; only OPEN frontiers support candidates. No new numerical constant - the patch radius derives from the frozen `edge_band_fraction=0.04`. `[fsg6e-check] passed=13 failed=0`, all fourteen negatives exit 1 (including `rawtermination`, `forget_history`, `stereo_hole`), all eighteen prior suites green, `SURFACE_FRONTIER`/`TARGETS` **exactly equal** to FSG6d and `inspect.getsource` confirms all eleven settled FSG6d helpers text-identical. **The central claim was demonstrated on real acquisitions**: both `closure_down_left` trials terminated **`no_frontier` with the raw frontier still at 169-170 surfels** while OPEN collapsed to 6-9 and every unvisited direction fell below the frozen 8 (per-direction raw 8-100, OPEN 0-7) - the observer decided exploration was complete from its own 3D memory and completed binocular observations, which FSG6d could not do. Those two trials passed with empty fail lists (98.64/98.84% coverage, 4.51 mm median). **`closure_up_right` failed on both seeds, for two different reasons.** Seed 1123: one fail line, termination - OPEN fell 124→16 and candidates 5→1, but `(12,-2)` survived with 96 raw aligned surfels of which 82 were BOUNDARY_RESOLVED and 3 MAP_RESOLVED, leaving **11 OPEN against a minimum of 8**. Seed 1181: at fixation 3 the candidate `(12,13)` won on `predicted_new_angular_area` (134.26 vs 103.65/73.04/15.09) despite the **weakest** support of the four - **8 OPEN, exactly the minimum** - and the weakest corridor; `(12,13)` is past the fixture's +9.942° pitch bound so fixations 4-5 fell off the ribbon (coverage 0.8268/0.6650). The seeds diverge **on a knife edge**: the same `(12,13)` corridor fraction is **0.1490 vs 0.3230**, straddling the frozen 0.15, computed from only **six** projected rays with the right eye at exactly 0.0000 in both. **A specification result, not an implementation defect - no code fix was made**: the OPEN filter works (170→9 overall; 25→8 on the decisive candidate); what fails is its interaction with three frozen pieces - a six-ray corridor sample, the new-area primary sort key, and a minimum support of exactly 8. **The termination mechanism is sound; what is unestablished is robustness.** That robustness gap was then closed by FSG6f, which PASSED 4/4 and closed Increment 6; see the FSG6f row below. See `docs/fsg6e-increment6.md`. |
| FSG6f: candidate frontier-state consensus (Increment 6) | **COMPLETE - Increment 6 CLOSED 2026-09-20 (`FSG6F_INCREMENT6_PASS`, 4/4 trials, every fail list empty).** FSG6f changed exactly one abstraction: candidate-level aggregation of the frozen FSG6e surfel states. A direction is exploration-open only when **`N_OPEN > N_MAP + N_BOUNDARY`**, independently retaining the frozen `N_OPEN >= 8` gate; **a tie is resolved, not open**. This is state consensus, not a fitted threshold - the check suite asserts the source contains no `0.5`. Everything else stayed frozen and was verified: `SURFACE_FRONTIER`/`TARGETS` **exactly equal** to FSG6e, fourteen settled helpers **text-identical** by `inspect.getsource` (the whole FSG6e MAP/BOUNDARY/OPEN classifier and every FSG6d corridor helper), and the candidate sort key asserted by literal string match. `[fsg6f-check] passed=14 failed=0`, all fifteen negatives exit 1 (including `anyopen`), all nineteen prior suites green. On two fresh non-mirror ribbons x seeds 1237/1291 (4,613,734,400 samples) **all four trials terminated `no_frontier`** - `consensus_up_right` in six fixations at **99.40/99.44% coverage, 4.32/4.31 mm median**, `consensus_down_left` in **five** at 98.52/98.54% and 4.38/4.39 mm - with pitch span 15.0 deg, per-patch coverage 0.9053-0.9547, gain 60.9-62.2 pp, pure instance 141, every replay idempotent, and exported-cloud median radius **0.75043-0.75044 m vs true 0.750** and 0.70797-0.70800 m vs 0.710. **The new rule is load-bearing, not cosmetic**: 20 consensus rejections, **every one of which the retired FSG6e rule would have accepted** (all had corridor allowed and OPEN>=8), and **three of four trials terminate because of it**. It catches the exact FSG6e pathologies on fresh data - `(-3,-7)` at OPEN 11 vs resolved 85 of 96 (the 1123 survivor's shape) and `(-7,-13)` at **OPEN 8, exactly the frozen minimum**, vs resolved 100 of 108 with a 112.3 new-area (the 1181 off-ribbon move's shape) - while never blocking a productive move (selected candidates ran 38:0 to 76:7). One termination turned on an **exact 40/40 tie**. **No code fix was required or made; no source file was modified.** Scope: fresh single convex visible curved surfaces with oracle segmentation - NOT self-occlusion, hidden-surface discovery, multiple objects, head motion, learned gaze, optimality or calibrated uncertainty. The route is preserved as five formal FAIL records (FSG6a-e), each fixing one abstraction without tuning a constant. **The next experiment is authorized but NOT implemented.** That next experiment opened as FSG7a (Increment 7, head motion), which FAILED 0/4 on an inherited depth bias; see the FSG7a row below. See `docs/fsg6f-increment6.md`. |
| FSG7a: prescribed head motion (Increment 7) | **FAILED 2026-09-20 (`FSG7A_HEAD_MOTION_FEASIBILITY_FAIL`, 0/4 trials). Preserved and returned to Luiz/Chat; FSG7a feasibility is NOT closed and active head-motion selection remains not implemented.** Increment 7 opens on a point of physics: **true self-occlusion cannot be revealed by eye rotation at fixed centres** - changing fixation changes which rays are sampled, not which world points lie on a line of sight - so every increment through FSG6f was, in principle, unable to discover hidden surface. FSG7a therefore changes one physical assumption: the rig **translates laterally** (prescribed ±0.45 m, no motion policy) while **H0, the initial head frame, stays the persistent map frame**; stereo is reconstructed in Ht by the unchanged FSG1 instrument and transported by `x_H0 = x_Ht + t_H0` before the frozen 12 mm fusion. The head origin moves through a `make_calibration` keyword that **already existed** in the frozen geometry module, so no instrument change was needed. `[fsg7a-check] passed=7 failed=0`, all six negatives exit 1, all twenty prior suites green. **The mechanism worked on every trial**: direct evaluator return visibility **0.0000/0.0000 both eyes at H0 → 1.0000/1.0000 after translation**, reconstructed return coverage **4.7% → 87.5-96.2%** (gain 82.8-91.4 pp), newly visible measurements fused into H0 at **2.5-2.8 mm median overlap** with idempotent replay and **no ICP**; transport exact to 1.49e-08. Every gate passed on all four trials **except one, the same one each time**: final folded-surface median 12.62-12.98 mm against ≤10 mm. Root cause, measured: split by panel, the **return wing is accurate (1.60-2.33 mm median)** while the **front panel carries a uniform -13.7 mm depth offset** and holds 87-89% of surfels. At Z=2.50 m that implies a disparity bias of **-0.1687 px**, within 7% of the **-0.1579 px SGBM bias FSG1 itself measured** - and the fixture sits **0.40 m beyond the prescribed 2.10 m vergence**, further than any earlier FSG target. The return wing escapes it because its normal is ±x, so a depth offset slides along it. A diagnostic-only global +13.74 mm correction gives 3.66-3.78 mm median. **A measurement result, not an implementation defect - no code fix was made and no source file was modified**; correcting it would mean changing the frozen instrument or the vergence. See `docs/fsg7a-increment7.md`. The moving-head branch is DEFERRED; Stage II returns to fixed head / static scene, opening with Scene-1a below. |
| Scene-1a: seeded multi-object scheduling (Stage II) | **FAILED 2026-09-20 (`SCENE1A_STAGEII_FAIL`, 0/4 trials). Preserved and returned to Luiz/Chat; Scene-1a is NOT closed and no next Stage-II experiment is authorized.** Stage II returns to **fixed head, static scene** and asks whether three known seeded objects (IDs 201/202/203) can be maintained and scheduled autonomously in one scene. The **frozen FSG6f controller is reused by import, not copied** - `scene1a_policy.py` calls `object_policy.choose_next(...)` at a single site and defines none of FSG6f's frontier extraction, state classifier, consensus, corridor or ranking; an explicit diff over every FSG1-FSG7a source is empty. The one new abstraction is a **scene scheduler**: each object asks FSG6f, and the scene picks **lexicographically by largest predicted new angular area, then frontier score, then smaller instance ID**, with `scene_complete <=> every object independently reports no_frontier`. `[scene1a-check] passed=8 failed=0`, all seven negatives exit 1, all twenty-one prior suites green. **The mechanics all worked**: first three fixations exactly the prescribed seeds, all later selection autonomous, no repeated fixation, no object over the six-look budget, totals ≤18, every per-object map pure in its own ID, every fused patch idempotent, targeted overlap 2.01-5.11 mm median, and per-object surface accuracy inside the gates everywhere (median 4.37-5.20 mm, P95 13.15-18.25 mm). **The lexicographic rule was obeyed at all 39 autonomous decisions, 0 violations.** But all four trials ended `object_budget_exhausted`, never `scene_complete`. Root cause, measured: **a starvation fixed point in the primary key** - an unselected object does not acquire, so its map does not change, so **its bid does not change**. On `triad_a`, object 203's proposal is frozen at **area 105.48 / score 18.12 for all ten decisions**, below the 122-140 deg² the other two keep offering; it gets **zero autonomous attention**, ending 0.4726→0.4726 coverage with support histogram **{1: 23197} - not one multi-look surfel**. Decisively, **203 held the highest frontier score of the three at every decision** and would have won on the second key, but the first key never ties. Only 2 of 12 object-instances reached `no_frontier`; best coverage 0.7919 against a 0.90 gate. Opportunistic cross-object fusion fired but rarely (1-2 of 22-26 non-target patches per trial), honestly a property of these angularly disjoint base scenes. **No code fix was made and no source file was modified** - a faithfully implemented rule that fails is a specification result. **The memory and identity substrate works; attention allocation is what is unresolved.** That allocation question was then addressed as Scene-1b, which removed the starvation but FAILED on budget sufficiency; see the Scene-1b row below. See `docs/scene1a-stage2.md`. |
| Scene-1b: fair multi-object scheduling (Stage II) | **FAILED 2026-09-20 (`SCENE1B_STAGEII_FAIL`, 0/4 trials). Preserved and returned to Luiz/Chat; Scene-1b is NOT closed and no next Stage-II experiment is authorized.** Scene-1b changes only the scheduler: among **live** objects, only those with the fewest autonomous post-seed looks are eligible (`n_i == n_min`), and within that fairness class Scene-1a's ordering is kept exactly (area, then frontier score, then instance ID). No weight, timer, age bonus or learned term; FSG6f is imported unmodified. `[scene1b-check] passed=9 failed=0`, all eight negatives exit 1 (including `areaonly`), all twenty-two prior suites green, and an explicit diff over every FSG1-FSG7a **and** `scene1a_*` source is empty. **The abstraction worked.** Fair audit: **64 autonomous decisions, 64 rule-compliant, 0 violations**; **38 of 64 fairness-restricted and 31 of 64 where fairness rejected a strictly higher-area bid** — decisively load-bearing, not decorative. Service finished **perfectly equal at {201:5, 202:5, 203:5} in all four trials** with 13-14 attention switches: **Scene-1a's starvation fixed point does not occur anywhere**. Reconstruction improved sharply — **every** per-object surface, purity, multi-look and idempotence gate passed on all twelve object-instances (median **3.83-5.34 mm**, P95 15.52-16.90 mm, 12,681-20,406 multi-look surfels), **three object-instances reached exactly 1.0000 coverage**, ten of twelve passed the 90% gate, and `fair_triad_c/1789` came within **two** fail lines of passing. **Why it still failed is arithmetic**: 3 objects x 6 per-object looks = 18 = the global scene cap, so under lockstep fair service the global budget is exhausted precisely when the per-object budgets are, leaving no slack for an object needing one more look; only 5 of 12 object-instances reached `no_frontier`. Opportunistic cross-object fusion was **0** here (vs 1-2 in Scene-1a) because these objects are more separated and fair round-robin jumps between them — reported honestly, exercised but inert. **No code fix was made and no source file was modified.** **Fairness is settled; budget sufficiency is not.** That separation was then attempted as Scene-1c, whose component certification FAILED 1/12 so the ensemble was never run; see the Scene-1c row below. See `docs/scene1b-stage2.md`. |
| Scene-1c: certified composition (Stage II) | **FAILED 2026-09-20 (`SCENE1C_COMPONENT_CERTIFICATION_FAIL`, 1/12 component controls). Per the prospective rule the ensemble stage was NOT run — no ensemble smoke, no full ensemble trials, no composition diagnostic. All twelve controls preserved; Scene-1c is NOT closed.** Scene-1c adds **no runtime policy**: `scene1c_policy.py` imports `scene1b_policy as frozen_scene_scheduler` with **zero** direct FSG6f imports and aliases rather than reimplements all eight scheduler symbols; budgets, fusion and every target are **measurably identical** to Scene-1b. Its new element is a **protocol**: before any ensemble, certify each `(fixture, seed, object)` actor with a full-profile control that renders the **complete three-object fixture** (verified: all twelve manifests record `complete_three_object_scene_rendered: true`, and both runners invoke the same `scene1c_render_fix.py`), reconstructs one object under unchanged FSG6f, and must reach `no_frontier` inside six looks. `[scene1c-check] passed=10 failed=0`, all ten negatives exit 1 (including `incompletecert`, `schedulercopy`, `budgetbump`), all twenty-three prior suites green. **Only `cert_triad_e/1847/obj203` certified** — 4 looks, `no_frontier`, 0.6153→1.0000 coverage, 5.169 mm median, zero fails. Everything structural passed in all twelve (pure maps, idempotent replay, no revisit, ≤6 looks, medians 5.17-6.62 mm, P95 16.25-18.09 mm). **Three failure modes, cleanly separated**: object 203 on three of four (fixture,seed) combos took **one** autonomous look and stopped with **gain +0.0000**, moving toward the near edge its seed already covered while the certifying run moved into the unexplored far tail — the `e/1847` vs `e/1901` pair differs **only in Monte-Carlo seed** yet produced opposite first autonomous decisions and opposite outcomes; five controls hit `max_object_fixations` still reporting `continue`, three of them after already reaching ~1.0 coverage; and per-look measurement coverage fell to 0.8669-0.8994 in eleven of twelve. **No code fix was made and no source file was modified**; no actor replaced, reseeded or tuned. **The protocol earned its keep**: it caught actor-level unsolvability *before* spending ensemble runs, where Scene-1b's budget question had been confounded with component solvability. **The composition question is not reached** — its premise does not hold. The open question is now the frozen FSG6f controller's post-seed behaviour on this fixture family, not the scheduler. See `docs/scene1c-stage2.md`. |
| Reality Check 1: good enough on an ordinary static scene? | **OBSERVATION COMPLETE 2026-09-21 (`REALITY1_COMPLETE`, both records structurally valid, every `integrity_fails` empty). Not a PASS and not a FAIL: this run closes and reopens nothing, and no prior decision was edited.** After Scene-1c showed eleven of twelve actors unsolvable inside six looks, the open question moved off the scheduler and onto the object controller itself, so Reality Check 1 asks the pragmatic version: **does the frozen FSG6f mechanism produce a recognisable, metrically sane active reconstruction of one moderately irregular, mixed-texture target in a small cluttered static scene?** Fixed head, static scene, frozen FSG1 instrument, frozen FSG3 12 mm fusion, frozen FSG6f policy **imported not copied**, frozen six-look budget - **the only experimental change is the scene**: a shallow hanging cloth ~0.90 x 0.64 m at ~2.1 m, depth varying non-periodically by 8.7 cm, 120 rendered triangles, with a deliberately **mixed** texture (low-contrast panel std **0.0108** against printed-feature std **0.1171**) plus table, wall and two side props. **There is deliberately no numerical quality threshold**; the only automated FAIL condition is structural integrity. `[reality1-check] passed=6 failed=0`, all six negatives exit 1, all twenty-four prior suites green, and both manifests carry the same `public_spec_sha256`. **Seeds 2111 and 2179, each acquired once at `full`** (1,258,291,200 samples each): both ran the full six looks and both terminated `max_fixations`, never `no_frontier`. Seed 2111 reached coverage **0.2603 -> 0.5345**, median **6.188 mm** / P95 19.812 mm, measurement fraction min 0.8516, worst overlap median 3.025 mm, 21,927 multi-look surfels; seed 2179 reached **0.2604 -> 0.7329**, median **5.720 mm** / P95 18.106 mm, min 0.8400, worst overlap median 2.897 mm, 27,660 multi-look surfels. Structure held everywhere: truth never opened, map ids exactly {141}, every fused patch idempotent, 6 of 6 gazes unique, budget and fusion unchanged. **The seeds diverge completely**: from the same prescribed `(-6,-4)` start, 2111's first autonomous move is `(1,-1)` and 2179's is `(1,+1)`, they never reconverge, and they end **19.8 coverage points apart with nothing differing but the Monte-Carlo render seed** - the same sensitivity Scene-1c's object 203 showed, now isolated on a single object. Seed 2179's fifth look returned a new-point fraction of **0.001** and zero coverage gain. Yet the medians sit inside the **4.3-6.6 mm band every earlier FSG6/Scene increment produced**, and visually the map is **recognisable as the cloth, coherent rather than fragmented, at the right depth** - the central 98% of reconstructed depths spans 2.083-2.167 m against a true 2.083-2.166 m - **with no gross wrong-depth region; its deficiency is incompleteness, not misplacement** (half to three-quarters of the visible surface in six looks, the rest never visited). That reading is reported, not promoted to a criterion. **Three code fixes, all demonstrable implementation defects**: the check module's missing `sys.path` entry, which had been making all six negatives crash-pass rather than control; `render_seed` overflowing Blender's signed-32-bit Cycles seed for **every** gaze under seed 2179 (2,179,000,420 against a 2,147,483,647 limit, so 2179 crashed 2.5 s into fixation 0), folded into int32 range - **the identity over the whole of seed 2111's domain**, leaving the already-acquired 2111 record and the spec digest unchanged; and the range check that would have caught it, verified fail-capable. No scientific or numerical behaviour, constant, fixture or gate was touched; nothing was rerendered after a numerical result; no alternate seed was used; the crashed attempt is preserved. **Luiz/Chat decide whether this is good enough and what the next practical step is.** See `docs/reality-check-1.md`. |
| Reality Check 2: let the observer finish | **BLOCKED 2026-09-21 (`REALITY2_INTEGRITY_FAIL`, solely because the two full continuation records do not exist). The diagnostic smoke raised a RUNTIME EXCEPTION, which by the prospective rule blocks full acquisition, so NO full continuation was run and no comparison was produced. No structural integrity check failed anywhere and no FAIL line was produced. Preserved and returned to Luiz/Chat; Reality Check 2 is NOT closed.** Reality Check 1 ended both records on the inherited six-look limit while FSG6f still said `continue`, so Reality Check 2 asks the literal follow-up: **if those exact saved states are not interrupted, does the unchanged observer continue to useful new surface and stop by its own `no_frontier` rule?** One scientific change - the six-look interruption is removed; scientific stopping is exactly frozen FSG6f `no_frontier`, and **24 total fixations is an engineering watchdog only, reaching it reported and never a FAIL**. The first six views are **not rerendered**: each run loads the exact saved map, gaze history, binocular observation history and final `continue` decision, copies `maps/00..05` byte-for-byte, and resumes at the recorded `next_gaze_deg`. `[reality2-check] passed=7 failed=0`, all seven negatives exit 1 (including `rerenderparent`, `sixlimit`, `watchdoggate`), Reality Check 1 and FSG6f suites still green, the diff over every FSG1/FSG3/FSG6f **and Reality Check 1** source empty against `7c1bfc7`, and **32 of 32 parent conditions hold on each saved record**, unchanged afterwards. **The continuation machinery works**: resumption at `(14,6)` with no Reality Check 1 view rerendered, and **seven further looks acquired, fused and replayed successfully** - coverage **0.5276 → 0.7904, +26.3 points**, 22,080 → 33,775 map points, 12,282 multi-look surfels, median/P95 16.701/46.167 mm on `small`, map pure in {141}, thirteen unique gazes, every patch idempotent. **So continuing is not futile** - that part is answered. **Then at step 13 frozen FSG6f selected `(-16,+6)`, 3.5° beyond the target's left edge** (target yaw span [-12.54,+12.93]), giving **zero** target pixels against 2,495-6,255 on each of the seven preceding looks, and the guard `if len(p.xyz_h) < 100` - **verbatim inherited from `reality1_run.py`** - aborted the run. Diagnosed read-only, three ways: the parent state reconstructs **bit-exactly** (calibration key-for-key identical to the live read, all four mask arrays reproducing from the saved patches, maps byte-identical); the decision **replays deterministically offline** from `map_12.npz` and the saved history with no renderer involved; and it is **the specified ranking doing what it specifies** - `(-16,+6)` won on predicted new area **128.93 deg²** despite the lowest corridor fraction (0.327 vs 0.898 and 1.000), the lowest frontier score and the only non-zero resolved-boundary count, because the frozen key is `(-area, -score, |dyaw|+|dpitch|, yaw, pitch)` and strict OPEN-majority passes 77 > 0+21. **No code fix was made and no source file was modified** - deciding what an off-object look means in a continue-until-`no_frontier` regime is a change to the experiment's stopping semantics, explicitly reserved. Nothing rerendered, no alternate seed, no constant touched; the blocked smoke is preserved. **Whether FSG6f ever reaches `no_frontier` on this fixture remains unknown**, and the record now holds **two independent demonstrations that FSG6f's area-first ranking can walk off a fixture** - FSG6c's `max()` case at three looks and this one at thirteen - evidence about the object controller, not the scheduler or the scene. See `docs/reality-check-2.md`. |
| Reality Check 2b: learn from an empty look | **COMPLETE 2026-09-21 (`REALITY2B_COMPLETE`, both records structurally valid, every `integrity_fails` empty, no FAIL line anywhere). Both seeds terminated by the scientific rule, `no_frontier`; neither reached the watchdog. Not a PASS/FAIL - an observation.** Reality Check 2 was blocked when frozen FSG6f chose an off-target gaze and the inherited `<100 target points` guard raised a runtime exception. Reality Check 2b makes **exactly one semantic change**: such a fixation records its gaze as visited, appends its completed binocular masks and support to observation history, **fuses zero target points and leaves the persistent map unchanged**, and is handed back to the unchanged FSG6f policy. FSG6f ranking and constants, the `<100` condition itself, scene, texture, stereo, fusion, seeds, vergence, the exact six-look parents and the 24-look watchdog are all frozen; `tools/reality2_render_fix.py` is reused unchanged. `[reality2b-check] passed=7 failed=0`, **all ten negatives exit 1** (`abortempty`, `skipempty`, `dropgaze`, `fuseempty`, `sixlimit`, `rerenderparent`, `policycopy`, `truth`, `qualitygate`, `watchdoggate`), reality1 6/6, reality2 7/7 and fsg6f 14/14 with **15/15** fsg6f negatives firing, and the diff over every FSG1/FSG3/FSG6f **and Reality Check 1/2** source empty against `27cfcc2`. **The one change was sufficient**: seed 2111 finished in **13 fixations** (7 new, 1 empty) at coverage **0.5345 → 0.7984** with median **5.878 mm** / P95 18.599 mm and **48,350 multi-look surfels (41.1%)**; seed 2179 in **16** (10 new, 2 empty) at **0.7329 → 0.8834**, median **5.812 mm** / P95 18.858 mm, **57,571 (41.7%)**. Recovery is directly evidenced, not inferred: at seed 2179 step 14, gaze `(-16,-4)`, a look returning **25 target points from 317 reference pixels** left the map **bitwise identical** and frozen FSG6f's immediate response was **`continue`** with a new gaze and two surviving candidates — the observer incorporated a near-empty look and kept going; seed 2111's single empty look (**0 points from 0 reference pixels**) resolved the frontier at once. Structure held everywhere: parent maps **byte-identical**, **not one of the twelve saved Reality Check 1 views rerendered**, truth never opened, map ids exactly {141}, every *fused* patch idempotent (12/12 and 14/14) with empty entries correctly claiming no idempotence, all gazes unique. **Seed divergence largely stopped being a quality question**: the coverage gap **narrowed from 0.1984 at look 6 to 0.0850**, medians differ by 0.066 mm, and both maps are ~41% multi-look, while the trajectories stay entirely different. **No code fix was made and no source file was modified** — every failure mode the authorization listed was checked against the records and none occurred. **What it does not establish is completeness**: `no_frontier` here means *no open frontier reachable from the perimeter I walked*, not *the surface is finished* — seed 2111 stops with a **large unvisited rectangular hole in the middle of the cloth**, most of its missing 20%, and **five of the seventeen new fused looks returned under 1% new points**. The open question moves back onto the frozen controller: why FSG6f's frontier/consensus rule treats an enclosed interior hole as resolved, and why area-first ranking keeps spending full-cost looks on sub-1% returns. See `docs/reality-check-2b.md`. |
| Cyclopean-1a: spherical topology hole probe | **COMPLETE 2026-09-21 (`CYCLOPEAN1A_COMPLETE`, `structural_fails: []`, no FAIL line anywhere). Structural only - no PASS is inferred from coverage or hole reduction. Not a PASS/FAIL: an observation.** Reality Check 2b ended both seeds by `no_frontier` and exposed the next missing abstraction: `no_frontier` can leave an unsampled region behind. Cyclopean-1a is the first concrete use of the fixed-head **cyclopean sphere as a 2-D perceptual organization layer** over the metric surfel map, which stays authoritative — the chart is bookkeeping. Target surfels project to `yaw=atan2(x,-z)`, `pitch=atan2(y,hypot(x,z))` on D9's declared `2*s0` grid (**0.1 deg** at full), each with an angular footprint derived from the **frozen 12 mm FSG3 radius**, `atan(0.012/median_range)` = 0.322 deg → **4 cells**, so **no new metric tolerance enters**. An internal hole is a complement component not touching the padded border; the exterior is not a hole. `[cyclopean1a-check] passed=6 failed=0`, all six negatives exit 1 (`fillhole`, `physicalclose`, `exteriorhole`, `truth`, `mesh`, `multiprobe`), reality2b 7/7 (10 negatives), reality1 6/6, fsg6f 14/14 (15/15), and the diff over every FSG1/FSG3/FSG6f **and Reality Check 1/2/2b** source empty against `077850d`. Parents located **by manifest**, 23/23 and 26/26 conditions, hashes pinned and **re-verified unchanged after the probes**. **The chart discriminates**: on the same fixture and settings it returned **zero** internal holes for seed 2111 and **exactly one** for seed 2179. Seed 2111 — complement is a single **border-touching** component, so no hole, **no probe**, map **bitwise unchanged**, nothing rendered; a valid observational result. Seed 2179 — one internal hole of **772 cells / 7.7165 deg²** at (+6.278,-1.587), state `UNOBSERVED_HOLE` with **zero** observed cells of either kind inside; centroid snapped to **(6.3,-1.6)**, inside the domain and not a revisit; one fixation returned **58,721 target points** — the largest single-look count in the series — fused **3,174 new surfels**, replay-idempotent, map 138,010 → 141,184, multi-look 57,571 → **70,136**, ids exactly {141}; the selected hole went **7.7165 → 0.3498 deg²**, reported with no threshold attached. **It also corrected a Reality Check 2b description**: seed 2111's gap, called an enclosed interior hole there from an (x,y) surfel scatter, is **topologically open to the exterior** through a channel on the left — a bay, not a lake; the RC2b numbers are unaffected. Discretization evidence, reported before touching any raster rule: the **undilated** complement has **330** and **410** internal components, the largest spurious one **0.4168 deg²**, all removed by the inherited footprint while seed 2179's genuine 13.88 deg² component survived — **no raster rule was changed**. **No code fix was made and no source file was modified** (a `NaN→int64` cast RuntimeWarning was diagnosed and measured to admit **zero** non-finite entries, so it changes no number and was reported rather than edited). **What it does not settle**: the **physical-depth-break cue was never exercised in the field** — the only hole was unobserved, so its range-gap test returned `None` and decided nothing; and whether topology should inform stopping is untouched, since Cyclopean-1a is one probe by construction and an open bay is not a hole by the stated rule. See `docs/cyclopean1a.md`. |
| Cyclopean-1b: spherical shoreline audit | **COMPLETE 2026-09-21 (`CYCLOPEAN1B_COMPLETE`, `structural_fails: []`, no FAIL line anywhere). Structural only - no PASS is inferred from arc counts, shoreline length or penetration depth. Not a PASS/FAIL: an observation.** Cyclopean-1a corrected seed 2111 from "internal hole" to exterior-connected **bay**, which left a gap in the vocabulary: a hole is nameable and probeable, **a bay is neither** - yet it is what `no_frontier` leaves behind. Cyclopean-1b is a **read-only** audit of the *shoreline* - complement cells 8-adjacent to target support, each keeping its component identity (`INTERNAL` / `EXTERIOR`) and a state from completed prediction-side evidence only (`UNOBSERVED`, `TARGET_CONTINUATION`, `PHYSICAL_DEPTH_BREAK`, `AMBIGUOUS`), with **no minimum-arc filter, no smoothing, no morphology tuning**. **Nothing was acquired**: no Blender process (verified - no `subprocess`/`bpy` reference, no process, no `acquisition/` output), no fixation, no fusion, 2.3 s and 2.9 s Interactive. `[cyclopean1b-check] passed=6 failed=0`, all six negatives exit 1 (`holeonly`, `exteriorresolved`, `depthblind`, `truth`, `mesh`, `acquire`), cyclopean1a 6/6 (6/6), reality2b 7/7 (10/10), reality1 6/6 (6/6), fsg6f 14/14 (15/15), and the diff over **33 frozen sources including every Cyclopean-1a source** empty against `f7f3b05`. Parents located **by manifest**, hashes pinned and **byte-identical afterwards** (seed 2111's `map_before` and `surface_map` share one sha256, independently reconfirming 1a's "no probe"). **The chart named the bay without being asked to look for one**: seed 2111's bay is a **single connected `UNOBSERVED` arc of 615 cells - 40.6% of the whole shoreline** - at (-1.078,+2.015), span 20.5x8.9 deg, exterior penetration depth **[5, 132, 209] cells (20.9 deg)** while the **second**-deepest of 213 exterior arcs reaches only **22**: a **9.5x** separation **no threshold produced**. Lakes and bays stay distinct - seed 2179's 35-cell residue from the 1a probe survives as the lone **`INTERNAL`** arc (33 cells at (+6.852,-2.136)) while its staircase notch stays `EXTERIOR` at depth 45. **The depth-break cue that 1a recorded as never exercised is exercised here**: `PHYSICAL_DEPTH_BREAK` gaps run to **1.59 m** and **1.74 m** (the room behind the table) with minima 0.0121/0.0132, while every `AMBIGUOUS` arc with a defined gap sits **below** the frozen radius (max 0.0118/0.0114) - **the inherited FSG3 12 mm scale falls in the empty interval between two measured populations**, and 63.0%/68.2% of physical shoreline cells lie below pitch -6.0 deg against 3.5%/3.2% of unobserved ones. **Two of four states did not occur and were diagnosed, not repaired**: `AMBIGUOUS` is entirely the sub-12 mm tail (zero cells carry both kinds of evidence), and `TARGET_CONTINUATION` is **0 arcs and structurally unreachable** - **100.00%** of the 28,835/34,880 target-evidence cells fall inside the dilated support, because a fused observation becomes a surfel whose 12 mm footprint covers the cell it projected to; the four-state vocabulary is really **three-state** on this fixture. The shoreline is genuinely **fragmented** (213 and 195 arcs, median arc **2** and **1** cells) because a minimum-arc filter is forbidden - reported as measured, **nothing smoothed after seeing it**. **No code fix was made and no source file was modified**: the inherited `NaN->int64` warning was re-proved harmless both by inspection (`_indices` filters on an **explicit** `isfinite` term) and by measurement (a NaN-prefiltered rebuild gives **bitwise identical** evidence and support on both seeds), and the `-1` unreachable-distance sentinel was verified **never to reach the report**. **What it does not settle**: **no policy changed** - no probe selected, no gaze proposed, no stopping rule touched; **no completeness claim** - 40.6% describes the shoreline, not the object, and `no_frontier` is not called wrong; and whether a deep unresolved arc should ever become a fixation is **deliberately unanswered**. See `docs/cyclopean1b.md`. |
| Cyclopean-1c: one deep-bay probe | **COMPLETE 2026-09-21 (`CYCLOPEAN1C_COMPLETE`, `structural_fails: []`, no FAIL line anywhere). Structural only - no PASS is inferred from target points, map growth or bay reduction. Not a PASS/FAIL: an observation.** Cyclopean-1b could *describe* seed 2111's bay but deliberately would not act on it. Cyclopean-1c takes **exactly one** fixation at the bay's deepest cyclopean complement cell, seed 2111 only, **with FSG6f never consulted and the stopping policy untouched**. `[cyclopean1c-check] passed=6 failed=0`, all six negatives exit 1 (`internal`, `physical`, `centroid`, `multiprobe`, `truth`, `policy`), cyclopean1b 6/6 (6/6), cyclopean1a 6/6 (6/6), reality2b 7/7 (10/10), reality1 6/6 (6/6), fsg6f 14/14 (15/15), and the diff over **38 frozen sources including every Cyclopean-1a and 1b source** empty against `695dbd9`. Parent located **by manifest** (exactly one seed-2111 1b record), ancestry followed 1b -> 1a -> RC2b, all hashes pinned and **byte-identical afterwards**. **The selection was derived from the record, not restated**: one eligible EXTERIOR component (18,116 cells, **1,059** UNOBSERVED shoreline cells reaching depth **209**), deepest cell (y=109,x=209) at depth 209 with 3 cells tied, gaze **(8.0, 2.1) deg**, **no revisit fallback**, step `fix_13`. One Blender launch, **one** fixation, zero parent rerenders: **52,873 target points** fused into **20,167 new surfels** (32,706 matched) - a **17.15%** gain on a map `no_frontier` had already called finished - replay-idempotent, ids {141}, map **117,567 -> 137,734**, verified independently by reproducing the fusion bitwise. **The bay is measurably reduced**: max penetration depth **209 -> 168**, complement **18,116 -> 13,366**, support 34,221 -> 38,971, UNOBSERVED shoreline **1,059 -> 918**, cells deeper than 168 down to **zero**; the deepest unobserved arc shrank 615 -> 454 cells. **But one probe did not finish it, and it created structure of its own**: the bay stayed **exterior-connected** through its narrow entrance channel, and internal components went **0 -> 2** (31 cells at (+6.790,-2.148) and 1 cell at (+7.300,-2.900)) - filling a bay from one viewpoint turns part of open water into enclosed lakes. **The residue is an instrument limit, not a sampling gap**: the 31-cell hole was traced into the probe image to pixel (228,111), whose neighbourhood is **10.4% valid against 82.2% frame-wide** with local texture **0.00117 vs 0.00992** and mean RGB [1.094,0.263,0.184] - the saturated, untextured emblem printed on the cloth, where the frozen SGBM instrument yields no disparity, so **bay depth alone would keep proposing looks at a spot the instrument cannot resolve**. Map coherence: new surfels **entirely inside** the old range envelope, pre-existing surfels moved by at most **5.94 mm** (p99 1.72), **61.9%** of probe points associating within the frozen 12 mm radius. **No code fix was made and no source file was modified** (the inherited `NaN->int64` warning was re-proved harmless by inspection and by a bitwise-identical prefiltered rebuild; one "disagreement >12 mm" statistic was computed and **discarded as definitionally forced**). **What it does not settle**: **no stopping rule changed and none is proposed**; nothing shows one probe is enough or that more would converge (a second was forbidden by construction); and **no quality claim is made** - 20,167 surfels is a count, no evaluator truth was opened, so the new surface's correctness is unmeasured. See `docs/cyclopean1c.md`. |
| Cyclopean-1d: observation versus measurement | **COMPLETE 2026-09-21 (`CYCLOPEAN1D_COMPLETE`, `structural_fails: []`, no FAIL line anywhere). Structural only - no state is a PASS and none selects a fixation. Not a PASS/FAIL: an observation.** Cyclopean-1c left a residue its own diagnosis called *seen but not measured*, exposing a flaw in the vocabulary: the 1b state `UNOBSERVED` means only "no valid 3D evidence landed here", merging **absence of attention** with **failure of measurement**. Cyclopean-1d is a **read-only** refinement of exactly those cells, replaying **14 completed fixations** (`fix_00`..`fix_13`) through saved calibration, rectification, oracle instance masks and the **frozen stereo `valid` mask** - observation and depth kept as independent fields, **no texture threshold**. **Nothing acquired** (no `subprocess`/`blender`/`bpy` token, no process, three host artifacts only), 2.6 s Interactive. `[cyclopean1d-check] passed=6 failed=0`, all six negatives exit 1 (`depthonly`, `acquire`, `truth`, `policy`, `threshold`, `mutateparent`), cyclopean1c/1b/1a 6/6 (6/6 each), reality2b 7/7 (10/10), reality1 6/6 (6/6), fsg6f 14/14 (15/15), and the diff over **44 frozen sources including every Cyclopean-1a/1b/1c source** empty against `b32ce44`. Parent by manifest, ancestry followed to its root (Reality1 steps 0-5, RC2b 6-12, 1a none, 1b none, 1c step 13), all pinned hashes **byte-identical afterwards**. **The projection was validated before it was trusted** - the shipped self-test uses an identity `R_hc` and cannot confirm the convention on real data, so round-tripping each view's own reconstructed points gave **0.0000 px median, p95 and max** reprojection error on all three ancestry branches; **no empirical offset was introduced**. **The 918 base-`UNOBSERVED` cells split exactly, along an anatomical line**: `NEVER_OBSERVED` **388** (42.27%, one arc at (-3.621,+2.306), depth 168 - and all **5,432** projection attempts fell **entirely outside every view's rectified core**, checked rather than assumed); `OBSERVED_TARGET_NO_DEPTH` **28** (3.05%, **every one internal**); `OBSERVED_NONTARGET_ONLY` **398** (43.36%, all on the outer rim - imaged, and the object ends); `NO_RANGE_REFERENCE` **104**; `OBSERVED_TARGET_WITH_DEPTH` and `MIXED_OBSERVATION` both **0**. **The Cyclopean-1c emblem diagnosis is confirmed from the instrument's own valid mask**: 27 cells at (+6.7926,-2.1519) plus 1 at (+7.3000,-2.9000), **27+1 target-seen and 0 depth-valid**, and of the 14 views **exactly one - `fix_13`, the 1c probe itself - contributed any supported projection there**. `NO_RANGE_REFERENCE` is a deterministic artifact: all 104 cells sit at **5.10 cells** from raw support, just past the inherited 5-cell disk - **left unadjusted**, since widening it would be a new tolerance. **No code fix was made and no source file was modified** (the inherited `NaN->int64` warning was re-proved harmless against the **refined state array itself**, bitwise identical). **What it does not settle**: **no controller changed** - no gaze proposed, no ranking touched, FSG6f never imported; **no completeness claim** - 42.27% describes a shoreline, not an object; **the continuation point stays a hypothesis**, so `OBSERVED_NONTARGET_ONLY` is evidence about that hypothesis at that assumed range, not proof the surface ends; and **what a controller should do differently for never-seen versus unmeasurable boundary** is exactly what 1d refuses to answer. See `docs/cyclopean1d.md`. |
| Cyclopean-1e: epistemic gaze | **COMPLETE 2026-09-21 (`CYCLOPEAN1E_COMPLETE`, `structural_fails: []`, no FAIL line anywhere). Structural only - no surfel gain, coverage or depth change is a PASS threshold. Not a PASS/FAIL: an observation.** Cyclopean-1d split the old `UNOBSERVED` into distinct causes and refused to act on them. 1e permits **exactly one** action: `NEVER_OBSERVED` + `EXTERIOR` -> **deepest inherited border distance** -> one foveation, seed 2111 only, **with `OBSERVED_TARGET_NO_DEPTH` explicitly ineligible** and **FSG6f never imported or consulted**. `[cyclopean1e-check] passed=6 failed=0`, all six negatives exit 1 (`nodepth`, `internal`, `centroid`, `multiprobe`, `truth`, `policy`), and **all 55 prior negatives still fire, none weakened** (cyclopean1d/1c/1b/1a 6/6 each, reality2b 7/7 (10/10), reality1 6/6, fsg6f 14/14 (15/15)); the diff over **49 frozen sources including every Cyclopean-1a..1d source** is empty against `23b36d6`. Parent located **by schema**, pinned hashes **byte-identical afterwards**, and the manifest's `epistemic_before` matches the 1d parent's published counts **field for field**. **The selection was derived from the record**: exactly one eligible exterior component (**388** `NEVER_OBSERVED` cells, depths 9-**168**), deepest cell (y=62,x=127) at **168** with 2 tied, gaze **(-0.2,-2.6) deg**, **no revisit fallback**, step `fix_14`. **The exclusion held, measured not assumed**: all 28 `OBSERVED_TARGET_NO_DEPTH` cells carry `exterior_distance = -1` in components **1 and 2, both INTERNAL**, failing the state test *and* the exterior test independently. One Blender launch, **one** fixation, zero rerenders: **54,623 target points** fused into **9,262 new surfels** (45,361 matched), map **137,734 -> 146,996** (+6.72%), replay-idempotent, ids {141}, reproduced bitwise independently. **It aimed at genuinely unseen territory and moved it**: `NEVER_OBSERVED` **388 -> 290**, max depth **168 -> 142**, complement **13,366 -> 10,995**, support **38,971 -> 41,342**. **And it ignored what it was designed to ignore**: `OBSERVED_TARGET_NO_DEPTH` **unchanged at 28**, both internal components bitwise the same objects at (+7.300,-2.900) and (+6.790,-2.148). Coherence without truth - new surfels **entirely inside** the old range envelope (zero outliers), pre-existing surfels moved by at most **5.99 mm**, **83.0%** of probe points associating within the frozen 12 mm radius, and the patch sitting only **8.7 mm** from its neighbours' median range, a smoother join than 1c's 28.6 mm - **coherence is not accuracy, evaluator truth stayed closed**. **No code fix; nothing outside the seven 1e files modified** (the inherited `NaN->int64` warning re-proved harmless by a bitwise-identical prefiltered rebuild). **What it does not settle**: **no stopping rule changed and none is proposed**; **one fixation on one seed shows nothing about convergence** - the slot did **not** close, 290 cells remain at depth 142, and a second gaze was forbidden by construction; **no quality claim** - 9,262 surfels is a count; and **nothing was learned about what to do with `OBSERVED_TARGET_NO_DEPTH`**, which the step deliberately walked around. See `docs/cyclopean1e.md`. |
| Cyclopean-1f: iterate epistemic gaze | **COMPLETE 2026-09-21 (`CYCLOPEAN1F_COMPLETE`, `structural_fails: []`, scientific stop `NO_ELIGIBLE_EXTERIOR_NEVER_OBSERVED`, no FAIL line anywhere). Structural only - no coverage, gain, depth or look-count number is a PASS gate. Not a PASS/FAIL: an observation.** Cyclopean-1e aimed **one** look and so could say nothing about repetition. 1f repeats that rule **unchanged** - the selector `tools/cyclopean1e_gaze.py` is byte-identical at `a3599ea14fc00e60` - asking only whether the observer exhausts eligible `EXTERIOR NEVER_OBSERVED` shoreline. **It did, in two looks**: **17 total against the 24-look watchdog**, stopping because the eligible set was **empty**, not because a guardrail fired. `[cyclopean1f-check] passed=6 failed=0`, all six declared negatives exit 1, and **all 61 prior negatives still fire, none weakened** (cyclopean1e/1d/1c/1b/1a 6/6 each, reality2b 7/7 (10/10), reality1 6/6, fsg6f 14/14 (15/15)); the diff over **54 frozen sources including every Cyclopean-1a..1e source** is empty against `8672bba`. Parent by manifest, pinned hashes **byte-identical afterwards**, and the runner's own guard reproduced the parent's published `NEVER_OBSERVED` **290** / max depth **142** before acquiring anything. **Step 15** gaze **(+1.3,+4.2)**, cell (y=130,x=142) at depth **142** = component max: **58,475** target points, **3,298** new surfels, `NEVER_OBSERVED` **290 -> 142**, max depth **142 -> 69**. **Step 16** gaze **(-6.0,+4.2)**, cell (y=130,x=69) at depth **69** = again the max: **50,010** target points, **5,196** new, `NEVER_OBSERVED` **142 -> 0**, max depth **69 -> None**. Both replay-idempotent, ids {141}, map **146,996 -> 155,490** (+8,494), and the **max depth of the whole exterior component fell 142 -> 22**, so no deep pocket survives. **The exclusion held for the whole loop, not just the first step**: `OBSERVED_TARGET_NO_DEPTH` was **28 at every iteration**, both internal components bitwise the same objects at (+7.300,-2.900) and (+6.790,-2.148). **The residual is described, not repaired**: 527 cells remain base-UNOBSERVED - **423** `OBSERVED_NONTARGET_ONLY` on the rim (depth min 0/med 9/max 22, the object's own edge), **28** the untouched emblem, **76** `NO_RANGE_REFERENCE` all at **5.10 cells** from raw support, just past the inherited 5-cell disk, **left unadjusted**. Coherence without truth: overlap agreement **94.4%** and **89.6%** within 12 mm, pre-existing surfels moved by at most **6.83 mm**, only **2** new surfels outside the old range envelope by **0.73/0.57 mm** (0.5% of 145.2 mm relief), and the replayed map exact in support/provenance/ids with `xyz_h` differing by at most **121 nm**, **below one float32 ulp** - **coherence is not accuracy, truth stayed closed**. **No code fix; nothing outside the seven 1f files modified.** **Recorded weakness**: this step's six `--negative` flags print and exit 1 **unconditionally** rather than injecting the mutation and checking the real code rejects it, unlike 1a-1e; the **positive** checks were verified genuinely fail-capable against mutated scratch copies (three independent invariants each producing their own FAIL), so what is asserted is enforced, but **these negatives would need rewriting to be relied on as controls**. **What it does not settle**: one rule on one seed; **not object completeness**, **not accuracy**; nothing about other seeds/scenes; **the empty-look branch was never exercised**; and `OBSERVED_TARGET_NO_DEPTH` is exactly where 1d left it - the loop's success consisted of stepping around it. See `docs/cyclopean1f.md`. |
| Cyclopean-1g: re-centered measurement probe | **COMPLETE 2026-09-21 (`CYCLOPEAN1G_COMPLETE`, `structural_fails: []`, measurement outcome **`DEPTH_STILL_ABSENT`**, no FAIL line anywhere). A diagnostic label, not a PASS/FAIL - no recovered-cell count is a gate and the branch closes either way.** Cyclopean-1f reached **attention completion** (zero exterior `NEVER_OBSERVED`), leaving a residue that is qualitatively different: **imaged as target, but no valid stereo depth**. 1g asks whether placing it at the **foveal centre** for one look recovers depth with **every other variable frozen** - matcher, baseline, vergence, render settings, chart, footprint, 12 mm fusion, every tolerance - so the experiment **isolates placement**. The candidate is the **inverse of 1e/1f**: only **`INTERNAL` + `OBSERVED_TARGET_NO_DEPTH`**, with `NEVER_OBSERVED` and exterior components **ineligible**. `[cyclopean1g-check] passed=6 failed=0`; **the six negatives are genuine source-mutation controls**, each exiting 1 **only because a previously-passing check now fails** and naming its detector (`external`/`unseen`->`internal_no_depth_only`, `multiprobe`->`one_probe`, `quality`->`binary_outcome_no_gate`, `rescueloop`/`truth`->`no_truth_or_rescue_loop`) - and an inert mutation on a scratch copy produced **`ERROR ... escaped detection`, exit 2**, not a silent pass, **restoring the property 1f lacked** (the 1f caveat stands recorded; 1f was not edited). All **67 prior negatives** still fire, none weakened; the diff over **59 frozen sources including every Cyclopean-1a..1f source** is empty against `a4d9b7d`, with `reality2_render_fix.py` byte-identical at `9f1433d189fbcbb5`. Parent by manifest with its stop condition asserted, pinned hashes **byte-identical afterwards**. Selected component **2** (**27** no-depth cells of 31), centroid-nearest cell (y=66,x=197), gaze **(+6.8,-2.2)**, fallback rank **0**. **The result is the cleanest form the negative could take**: **all 27** residue cells had a **supported projection** and **all 27 were imaged as target** - none outside the core, none on background - and **0 recovered valid depth**. **The re-centering worked; the instrument still returned nothing, so placement is not the cause.** Independently visible: with the residue centred, the central **32x32** window is **23.0%** valid against **86.7%** frame-wide, mean RGB **[0.951,0.459,0.331]** - the saturated, near-uniform emblem, no internal detail for a matcher. Fusion: **55,567** target points, **194 new** / 55,373 matched (**99.7%** overlap within 12 mm - the fovea was looking almost entirely at surface already mapped), map **155,490 -> 155,684**, replay-idempotent, ids {141}, reproduced **bitwise** (xyz max diff **0.00 nm**). **The residue's 28 -> 22 is bookkeeping, not progress, and the record says so**: 21 kept, **7 disappeared, 1 appeared**, and **all 7 disappeared because neighbouring fused surfels grew over them** - **not one cell left the state by being measured**. The residue persists as **23** cells at (+6.661,-2.139) and **2** at (+7.450,-2.100). **No code fix; nothing outside the seven 1g files modified.** **What it does not settle**: one look, one residue, one seed; it says nothing about whether a **different matcher, baseline, vergence, illumination or active pattern** would recover the depth - **none was tried, by design**; and it is **not accuracy**, truth stayed closed. `DEPTH_STILL_ABSENT` is not a failure of the experiment but the measurement it was built to take. **Disposition: residue recorded as unresolved under the current fixed-head stereo instrument and deferred; the next research stage is multiple objects**, declared beforehand and unchanged by this outcome. See `docs/cyclopean1g.md`. |
| MultiObject-1a: second object seed | **COMPLETE 2026-09-21 (`MULTIOBJECT1A_COMPLETE`, `structural_fails: []`, no FAIL line anywhere). Structural only - no point count, footprint size or overlap figure is a PASS gate. Not a PASS/FAIL: an observation.** Cyclopean-1g closed the single-object branch; this opens the next stage by adding **exactly one new difficulty - two foreground object entities in one fixed-head scene representation** - and nothing else. Object **141** is inherited **read-only** from 1g; object **143** is **declared in advance** (not discovered) and gets **one** prescribed seed foveation. `[multiobject1a-check] passed=6 failed=0`; the six negatives are **genuine source-mutation controls**, each exiting 1 because its mutation was detected and naming its detector (`sameid`->`two_declared_objects`, `multiprobe`->`one_second_object_seed`, `merge`->`separate_geometry`, `truth`/`autodiscover`->`no_truth_or_discovery`, `grow`->`growth_deferred`), **none exiting 2**. All **73 prior negatives** still fire, none weakened; the diff over **64 frozen sources including every Cyclopean-1a..1g source** is empty against `aa5ac98`. Parent found **by manifest** (schema/seed/profile/truth), pinned hashes **byte-identical afterwards**. **The seed was derived, not chosen**: all **18** completed fixations carried incidental id-143 evidence - **43,595** points -> **11,688** occupied 0.1-deg cells, spherical mean **(+2.401,-0.603)**, nearest **occupied** cell giving gaze **(+1.5029,-7.9404)**. **That cell is 7.392 deg from the mean, and the reason is itself a first multi-object finding**: the mean of 143's own evidence falls **inside 141's footprint and outside 143's**, because 143 wraps below and around the cloth rather than centring on it - the *nearest occupied cell* rule did exactly what it exists for. One fixation `fix_18`: **74.5%** valid, and **three** instances present among valid pixels (**141: 27,963, 142: 15,513, 143: 5,344**). **The entities stay separate**: 141 is a `SURFEL_MAP` of **155,684** points with ids exactly **{141}** and an unchanged file hash; 143 a `SEED_SURFEL_PATCH` of **5,344** points with ids exactly **{143}**; **the id-set intersection is empty**, separate files, neither fused. On the shared 0.1-deg chart 141 holds **37,654** cells and 143 **1,641**, **overlap 0** - **checked, not assumed**, since the bounding boxes *do* cross in pitch: over the **14** shared rows 141 holds 1,189 cells and 143 holds 447 with **not one claimed by both**, disjoint in yaw by **19-45 cells** in every row. **The seed is visually centred on 143** (centre pixel is instance 143, central 32x32 is **67.6%** id-143) with **141 also in view** (47.5% of frame); of 12,649 visible id-143 pixels **42.2%** carried valid depth. **A third object (142) was visible with 15,513 valid pixels - more than 143 itself - and deliberately left uninstantiated**, because discovery is deferred; the deferral is now visible in the measurements, not just the prose. **One code fix, in a new 1a file only, after diagnosis**: the runner read 1g's diagnostic label at the manifest top level, where 1g does not write it (1g stores it in `probe_result`, as its own comparator reads it), so validation aborted for **any** valid parent; the lookup was corrected to match the parent's reader. **Corrected, not weakened** - verified it still rejects a removed label, a bogus label, an absent `probe_result`, a flipped `truth_opened` and a changed seed. **What it does not settle**: **coexistence, not multi-object reconstruction** - 143 has one patch from one look and was not grown; **no scheduler, discovery or growth loop**; **no accuracy claim**, truth stayed closed; and **zero overlap is a property of this configuration, not an invariant** - these two objects happen to be angularly disjoint from this head, and nothing shows what would happen if they were not. **Next: grow 143 independently while 141 stays stable.** See `docs/multiobject1a.md`. |
| MultiObject-2c: grow the selected third object | **COMPLETE 2026-09-22 structurally (`MULTIOBJECT2C_COMPLETE`, `structural_fails: []`, no FAIL line anywhere) but terminated on the engineering watchdog - `termination_reason: object3_watchdog`, `scientific_stop_reached: false`. The watchdog is not scientific success.** MultiObject-2b instantiated object 142 and stopped; 2c adds the one difficulty left - grow it with the frozen mechanism - and nothing else. **The id is consumed, not declared**: parent found **by manifest**, `multiobject2c_run.py:55` reads `selected_object_id` from it, and **the literal `142` appears nowhere in any 2c source**. **The seed was reproduced, not trusted** - recomputing `valid & (instance_id==142)` on `fix_42` gives **38,020** points with `xyz` **byte-identical** to the saved 2b patch - and **reused, never rerendered** (`parent_fixations_rerendered: 0`). `[multiobject2c-check] passed=6 failed=0`; **all seven negatives are genuine source-mutation controls**, each exiting 1 and naming its detector (`handpick`, `crossfuse`, `copypolicy`, `priorhistory`, `legacyrenderer`, `globalwatchdog`, `texturegate`), **none exiting 2**; **all 16 prior suites green and 110 prior negatives still firing**; the diff over **75 frozen sources including both renderers and every Cyclopean-1a..1g and MultiObject-1a..2b source** is empty against `97ba8cf`. 23 added fixations at global steps **43-65** on the frozen 5-deg lattice, **4 m 21 s** wall (260.0 s loop) on OPTIX/RTX 4090. Object 142 grew **38,020 -> 310,884** surfels (**8.2x**), **pure {142} at every one of 24 maps**, **all 24 replay-idempotent**, **no empty looks** (so the `<100` negative-evidence branch was **not exercised**), multi-look 130,949, max support 8, range 1.3565/2.4113/4.0649 m. **141 (155,684, {141}) and 143 (42,988, {143}) byte-identical**, all **9 pinned inputs** unchanged, truth closed, no quality gate, no scheduler, generic renderer only. **Texture-recovery diagnostics, descriptive only** (`texture_diagnostics_are_gates: false`): per-look recovery **min 58.8% / median 70.3% / max 77.3%**, against object 143's **1.4-2.5%** - **they ranked nothing, stopped nothing, gated nothing**. 142's footprint grew **9,624 -> 62,784** cells (627.84 deg2), **now larger than 141 and 143 combined**, with **all pairwise and triple overlaps still 0**. `object_142_growth.png` shows genuine **area-filling** growth with **interiors filled solid**, the visible counterpart of ~70% recovery and the sharpest contrast with 143's edge-concentrated lacework. **No code fix; no file modified by the run.** **What it does not settle**: the scientific question is **open** - the policy returned `continue` at **all 24** decisions and never `no_frontier`, wanting (+9.0,-18.811) with **650 open frontier voxels of 679 (95.7%)**, proportionally *more* open than 143 at its watchdog (70.2%), so this shows only that growth does not terminate within 24 looks, **not where it would end**; **no object completeness**; **no accuracy claim**, truth stayed closed; **the empty-look branch remains untested**; **zero overlap is still not an invariant**; and **no discovery, scheduler, semantic ranking, mesh or interpolation** was introduced, with 143 still retained for revisit. **Next is a judgement for Luiz/Chat**: raise the object-scoped watchdog for 142, accept the current coverage, or move to another scene-level decision. See `docs/multiobject2c.md`. |
| MultiObject-2d: epistemic audit of the selected object | **COMPLETE 2026-09-22 (`MULTIOBJECT2D_COMPLETE`, `structural_fails: []`, no FAIL line anywhere; the comparator agrees and exits 0). Read-only and descriptive - no count is a gate.** MultiObject-2c left object 142 densely grown but stopped on the watchdog with the policy still saying `continue`; 2d asks what that frontier *means*, adding nothing. **The answer inverts the previous object's.** Of **2,055** shoreline cells: **986 (48.0%) `NEVER_OBSERVED`**, **628 (30.6%) already-resolved boundary structure** (`PHYSICAL_DEPTH_BREAK` 537 + `AMBIGUOUS` 91), 291 (14.2%) `NO_RANGE_REFERENCE`, **125 (6.1%) `OBSERVED_TARGET_NO_DEPTH`**, 25 `OBSERVED_NONTARGET_ONLY` - **7.9x more unseen than seen-but-unmeasured**, where object 143 measured **18.2x the other way** (830 vs 15,089) and object 141 reached **zero** exterior `NEVER_OBSERVED` at Cyclopean-1f. **Three objects, three remainders, one unchanged vocabulary: the epistemic bottleneck is a property of the object and the attention it received, not of the instrument** - 143's shoreline (20,799) *exceeded* its own footprint (17,947), lacework; 142's (2,055) is **3.3%** of its footprint (62,784), a filled region with a thin rim. Host-side only: **no Blender, no Cycles, no GPU**, **4.5 s**, `pgrep blender` 0 before and after and zero `subprocess`/`bpy` references. **All 56 pinned inputs byte-identical** (5 parent files, 3 object geometry sources, all **48** saved calibration/observation files); objects 141/143/142 pure at 155,684/42,988/310,884. **Id consumed, not declared**: `multiobject2d_audit.py:52` reads `selected_object_id` and the literal `142` appears **zero** times in all five 2d sources. `[multiobject2d-check] passed=6 failed=0`; **all seven negatives are genuine source-mutation controls** each exiting 1 with the named detector, **none exiting 2**, and the **exit-2 escape branch was verified live**; **21/21 prior suites green, 135/135 prior negatives firing**; the diff over **every tracked non-doc source at the parent commit - 272 files** is empty. Chart **612 x 171** at **0.1 deg**; footprint from the **unchanged 12 mm** radius at this object's range, `atan(0.012/2.4113) = 0.2851310 deg` -> **3 cells** (report matches to 1.7e-8 deg); raw support **62,784** reproduces 2c's published footprint exactly. **`TARGET_CONTINUATION` 0**, **`OBSERVED_TARGET_WITH_DEPTH` 0**, **`MIXED_OBSERVATION` 0**; internal remainder is 99 of 106 cells seen-but-unmeasured. **The unobserved frontier is six large arcs, not slivers** (median 181.5 cells, max 339; border depth max **59** = the chart-wide maximum), **every one with `supported_projection_samples` = 0**, while `OBSERVED_TARGET_NO_DEPTH` has **156 supported projections, 156 target-seen, 0 valid depth** - observation cleanly separated from measurement. **Structurally, the unobserved remainder is exactly where the gaze lattice did not go**: the 2c lattice visited yaw [-21.000,+24.000] pitch [-18.811,-8.811] on an object spanning yaw [-30.20,+31.00] pitch [-25.60,-8.50]; **100% of the 986 cells** lie in arcs outside that span, all below the lowest gaze row, and the frozen policy **never proposed a pitch below -18.811** in 24 decisions. Observed, not acted on. The image's **pixel census reproduces the report exactly** (all 104,652 cells): a solid grey slab, a thin continuous **blue** depth-break line along the whole top edge, a **red** unobserved rim left/right/bottom with a white notch at bottom-centre, and only **orange speckle** inside. Status from a literal zero-comparison: **`ATTENTION_INCOMPLETE_RETAIN_FOR_REVISIT`**; **`scene_disposition = MOVE_TO_NEXT_OBJECT`** unconditionally. **No code fix; no frozen source modified.** **What it does not settle**: it is **descriptive, not a diagnosis** - it does not show why the lattice stopped short, whether more looks would reach those arcs, or that the policy is deficient; **no counterfactual was run and nothing was tuned**; **no accuracy claim**, truth stayed closed; **no object completeness**; **no claim the 125 unmeasured cells are irreducible** (no alternate matcher, baseline, vergence or illumination was tried, by design); `NO_RANGE_REFERENCE` is bookkeeping. **Next: next-object selection from updated scene memory.** See `docs/multiobject2d.md`. |
| MultiObject-3a: next-object selection from updated memory | **COMPLETE 2026-09-22 (`MULTIOBJECT3A_COMPLETE`, `structural_fails: []`, no FAIL line anywhere; the comparator agrees and exits 0). One deterministic scene-level decision - no count is a gate.** MultiObject-2d released scene progress while retaining object 142 as incomplete; 3a repeats the MultiObject-2a rule **unchanged** on a **doubled memory**. **Selected object 145** on **13,047** accumulated valid-depth samples against runner-up **144**'s **2,436** - margin **10,611, 5.36x**, tie-break not exercised. **No seed fixation taken.** **The honest result is that the larger memory did not change the answer**: the only positive ids present anywhere in the 48 looks are 141-145, so the third object's history **exposed no new object**, and 145 was already the argmax on the old history alone. Measured counterfactual - old 18..41 **145 10,667 vs 144 1,662 (6.42x)**, new 42..65 **145 2,380 vs 144 774 (3.07x)**, combined **5.36x**: the new looks **narrowed** the margin without reordering anything. **Stability, not discovery.** Host-side only: **no Blender, no Cycles, no GPU**, `pgrep blender` 0 before and after, **7.2 s**, **no acquisition directory**. Evidence scope **48 looks, steps 18..65**, 0 duplicates, globally contiguous - old 1b2-r2 history (24, 18..41) plus 2b seed + 2c growth (24, 42..65); the scope **begins at global step 18** by contract, excluding the object-141 Cyclopean/Reality lineage. Instantiated ids **[141,142,143]** consumed at `multiobject3a_run.py:106` from the **current 2c scene graph**; **no scene id is declared in any 3a source**. Candidates (valid depth decides, visibility diagnostic only): **145** 13,047/183,164 = **7.1%**, depth at 8 of 9 visible steps; **144** 2,436/66,237 = **3.7%**, 5 of 5. **Independent recomputation** without the package selector or its path helpers matched **selected id, support, ordering and the full valid+visible table exactly**. `[multiobject3a-check] passed=6 failed=0`; **all six negatives are genuine source-mutation controls** with named detectors, **none exiting 2**, and the **exit-2 escape branch was verified live**; **22/22 prior suites green, 142/142 prior negatives firing, none weakened** (Cyclopean-1f caveat stands, 1f untouched); the diff over **every tracked non-doc source at the parent commit - 277 files** is empty. **All 105 pinned inputs byte-identical** (3 of 2d, 3 of 2c, 3 object sources, all **96** calibration/observation files); objects 141/142/143 pure at 155,684/310,884/42,988. Object 142's `ATTENTION_INCOMPLETE_RETAIN_FOR_REVISIT` was **carried forward and did not block the selection**. **No code fix; no frozen source modified.** **What it does not settle**: **no accuracy claim**, truth stayed closed - 13,047 counts usable stereo samples in saved views, not reconstruction quality; **not a saliency or importance ranking** - support is confounded with how often an object fell inside fixations aimed at *other* objects (145's support comes entirely from steps 34-38 and 58-60); **not a claim that 145 is measurable** - its **7.1%** valid fraction sits closer to object 143's difficult regime (1.4-2.5%) than to 142's (58.8-77.3%); **not exhaustive over the programme's history**; **no revisit schedule** for object 142. **Next: seed the selected object 145 with one prescribed fixation.** See `docs/multiobject3a.md`. |

Oracle object IDs, RGB-derived geometry, fixed head/cyclopean map frame. Direct
perspective patch acquisition, not a change to the Phase C/D OSL sensor or loop.
Increment 2 added two-patch accumulation into that fixed map, Increment 3 an
active frontier policy that selects and stops its own fixations, Increment 4 the
paired efficiency comparison against a fixed scan, and Increment 5 a curvature
stress test on a convex cylindrical ribbon. Increments 1-5 are closed. No
registration, meshing, multi-object switching, head motion or vergence control is
enabled, and the policy remains a 2D image-edge/map-yaw controller.
