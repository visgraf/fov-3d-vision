# Generated by the tools. Every one of these is rebuilt by a command in the README.
previews/
*.blend
*.blend1
scenes/calib_room/calib_room.targets.json

# Downloaded, and too big for git: workshop_8k.exr alone is 324 MB against GitHub's
# 100 MB per-file limit. Both are pinned so they come back byte-identical --
# the HDRI by the md5 in scenes/hdri/<slug>/asset.json, which stays tracked,
# and Classroom by the URL in the README.
*.exr
scenes/classroom/

# Local environment.
.venv/
__pycache__/
